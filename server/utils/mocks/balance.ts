import { Account } from '../../@types/prisonApiTypes'

// eslint-disable-next-line import/prefer-default-export
export const balances: Account = {
  cash: 300,
  currency: 'GBP',
  damageObligations: 10,
  savings: 200,
  spends: 100,
}
