{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "../dateSelection/index.njk" import transactionsDateSelection %}

{% macro transactionsTable(balance, dateSelectionRange, selectedDate, transactions) %}
<div class="govuk-tabs__panel">
  <div class="transaction__balances">
    <h2 class="govuk-heading-m govuk-!-margin-bottom-1">Current balance</h2>
    <p class="govuk-body govuk-!-margin-top-5">£{{ balance }}</p>
  </div>

  {{ transactionsDateSelection(dateSelectionRange, selectedDate) }}

  <div class="transactions">
    {{ govukTable({ head: transactions.head, rows: transactions.rows }) }}
  </div>
</div>
{% endmacro %}